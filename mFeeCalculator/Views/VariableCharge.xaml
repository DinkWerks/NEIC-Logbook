<UserControl x:Class="mFeeCalculator.Views.VariableCharge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
	<UserControl.Resources>
		<Style x:Key="Line Header" TargetType="TextBlock">
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="Margin" Value="0,0,16,4"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
		</Style>
		<Style TargetType="TextBox">
			<Setter Property="VerticalAlignment" Value="Center"/>
		</Style>
	</UserControl.Resources>

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>

		<TextBlock Text="{Binding Charge.Name}" Style="{StaticResource Line Header}"/>
		<TextBox Grid.Column="1" Text="{Binding Charge.Count}"/>
		<TextBlock Grid.Column="2" Text="{Binding Charge.Cost}" HorizontalAlignment="Right"/>
		<TextBlock Grid.Column="3" Text="{Binding Charge.TotalCost, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Right"/>
	</Grid>
</UserControl>
